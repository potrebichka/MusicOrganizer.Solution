@{
    Layout = "_Layout";
}
@using MusicOrganizer.Models;
@if (@Model.Count == 0)
{
    <h3>No artist have been found!</h3>
}

<ul>
    @foreach (Artist artist in Model)
    {
        <li><a href="/artists/@artist.Id">@artist.Name</a></li>
    }
</ul>

<a href="/search_by_artist/new">Search another artist/a>
<a href="/"> Go home</a>
